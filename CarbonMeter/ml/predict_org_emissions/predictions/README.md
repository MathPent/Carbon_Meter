# 📊 Predictions Folder

This folder contains all prediction output CSV files generated by the pipeline.

## File Naming Convention

- `predicted_emissions_30days_YYYYMMDD_HHMMSS.csv` - 30-day forecasts
- `predicted_emissions_180days_YYYYMMDD_HHMMSS.csv` - 180-day (6-month) forecasts

## CSV Structure

Each prediction file contains:
- `day_ahead` - Day number in the forecast (1 to N)
- `predicted_co2_kg` - Predicted CO₂ emissions in kilograms
- `estimated` - Flag (always 1 for predictions)
- All operational features used for prediction

## Usage

These files are automatically generated by `predict_future_emissions.py` and can be used with `visualize_predictions.py` for graph generation.
